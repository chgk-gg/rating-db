# Minimize WAL generation
wal_level = minimal
max_wal_senders = 0
archive_mode = off
synchronous_commit = off
full_page_writes = off
wal_buffers = 64MB
max_wal_size = 10GB
min_wal_size = 1GB
checkpoint_completion_target = 0.9

# Disable autovacuum during restore
autovacuum = off
